<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Playlist</title>
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="/css/navbar.css">
  <link rel="stylesheet" href="/css/playlist-form.css">
</head>
<body>
  <%- include('../partials/_navbar', { user: user, playlists: playlists }) %>
  <div class="playlist-form-container">
    <h2>Edit Playlist</h2>
    <form action="/playlists/<%= playlist._id %>?_method=PUT" method="POST" class="playlist-form">
      
      <label for="name">Playlist Name:</label>
      <input type="text" id="name" name="name" value="<%= playlist.name %>" required>

      <label for="description">Description:</label>
      <textarea id="description" name="description"><%= playlist.description || '' %></textarea>

      <div class="switch-container">
        <label for="isPublic">Public</label>
        <label class="switch">
          <input type="checkbox" id="isPublic" name="isPublic" <%= playlist.isPublic ? 'checked' : '' %>>
          <span class="slider"></span>
        </label>
      </div>

      <div class="switch-container">
        <label for="isCollaborative">Collaborative</label>
        <label class="switch">
          <input type="checkbox" id="isCollaborative" name="isCollaborative" <%= playlist.isCollaborative ? 'checked' : '' %>>
          <span class="slider"></span>
        </label>
      </div>

      <button type="submit">Save Changes</button>
    </form>

    <a href="/playlists/<%= playlist._id %>" class="cancel-link">Cancel</a>
  </div>
</div>
  <%- include('../partials/_footer') %>

</body>
</html>
